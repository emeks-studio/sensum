services:
  proof-server:
    image: "midnightnetwork/proof-server:latest"
    command: ["midnight-proof-server --network devnet"]
    ports:
      - "6300:6300"
    environment:
      RUST_BACKTRACE: "full"
